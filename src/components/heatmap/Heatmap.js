import React from 'react';

export default function Heatmap(props) {
  // The real business logic goes inside.
  const { value } = props;
  return (
    <p>
      heatmap placeholder:
      {value}
    </p>
  );
}
