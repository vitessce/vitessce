---
id: coordination-types
title: Coordination Types
sidebar_label: Coordination Types
slug: /coordination-types
---
import AutogeneratedCoordinationTypes from '../src/pages/_AutogeneratedCoordinationTypes';
import AutogeneratedCoordinationTypeConstant from '../src/pages/_AutogeneratedCoordinationTypeConstant';

## Overview

Vitessce defines several coordination types upon which views can be coordinated via the view config. The coordination types found on this page are all valid values for the keys of the [`coordinationSpace`](/docs/view-config-json/index.html#coordinationspace) object in the view config.

<AutogeneratedCoordinationTypes/>

## Constants

We provide the `CoordinationType` constant mapping from JavaScript object keys to coordination type strings.
For convenience, you may specify a shorter alias such as `ct` when the variable is imported.

```js {7-11}
import { CoordinationType as ct } from 'vitessce';
```

<AutogeneratedCoordinationTypeConstant/>