import { makeStyles } from '@material-ui/core/es/styles/index.js';

export const useStyles = makeStyles(() => ({
  vegaContainer: {
    display: 'flex',
    flex: '1 1 auto',
    overflow: 'hidden',
  },
}));
