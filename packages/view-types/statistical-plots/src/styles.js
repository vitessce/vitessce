import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
  vegaContainer: {
    display: 'flex',
    flex: '1 1 auto',
    overflow: 'hidden',
  },
}));
